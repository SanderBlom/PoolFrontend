//This file is used to store functions related to fetching data from the pool table. 
const { pool } = require("./dbConfig");
async function CheckTableAvailability(tableid) {
    //Here we need to make a function that ask the VisionSystemAPI if the requested table is in use or not.

    return true;


}
module.exports = {CheckTableAvailability};
